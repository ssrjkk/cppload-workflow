apiVersion: v1
kind: Service
metadata:
  name: {{ include "cppload-pro.fullname" . }}
  labels:
    {{- include "cppload-pro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "cppload-pro.selectorLabels" . | nindent 4 }}
