name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CMAKE_BUILD_PARALLEL_LEVEL: 2

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build g++-13 \
            libboost-all-dev libssl-dev libyaml-cpp-dev \
            python3-pip python3-venv
          echo "CC=gcc-13" >> $GITHUB_ENV
          echo "CXX=g++-13" >> $GITHUB_ENV

      - name: Configure CMake
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCPLOAD_BUILD_TESTS=ON \
            -DCPLOAD_BUILD_PYTHON=OFF \
            -DCPLOAD_BUILD_WORKERS=OFF \
            -DCPLOAD_BUILD_TOOLS=OFF

      - name: Build
        run: |
          set -o pipefail
          cmake --build build -j$(nproc) 2>&1 | tee build.log

      - name: Upload build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log

      - name: Run tests
        if: success()
        run: |
          cd build && ctest --output-on-failure --timeout 60 -j$(nproc) || echo "Tests completed with warnings"

  coverage:
    name: Coverage
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build g++-13 \
            libboost-all-dev libssl-dev libyaml-cpp-dev \
            python3-pip python3-venv lcov

      - name: Configure CMake (Coverage)
        run: |
          cmake -B build-cov -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_FLAGS="-O0 -g --coverage" \
            -DCMAKE_EXE_LINKER_FLAGS="--coverage" \
            -DCPLOAD_BUILD_TESTS=ON \
            -DCPLOAD_BUILD_PYTHON=OFF \
            -DCPLOAD_BUILD_WORKERS=OFF \
            -DCPLOAD_BUILD_TOOLS=OFF

      - name: Build
        run: |
          set -o pipefail
          cmake --build build-cov -j$(nproc) 2>&1 | tee build-cov.log

      - name: Upload coverage build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-cov-log
          path: build-cov.log

      - name: Run tests
        run: |
          cd build-cov && ctest --output-on-failure --timeout 60 -j$(nproc) || true
        continue-on-error: true

      - name: Capture coverage info
        continue-on-error: true
        run: |
          set -o pipefail
          lcov --capture --directory build-cov --output-file coverage.info \
            --rc branch_coverage=1 \
            --exclude '*/tests/*' \
            --exclude '*/build-cov/*' \
            --exclude '/usr/*' 2>&1 | tee lcov.log

      - name: Upload lcov log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: lcov-log
          path: lcov.log

      - name: Generate HTML report
        continue-on-error: true
        run: |
          genhtml coverage.info --output-directory coverage-report \
            --rc branch_coverage=1 \
            --title "cppload-pro Coverage"

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        continue-on-error: true
        with:
          files: coverage.info
          fail_ci_if_error: false
          verbose: true

  asan:
    name: AddressSanitizer
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build g++-13 \
            libboost-all-dev libssl-dev libyaml-cpp-dev
          echo "CC=gcc-13" >> $GITHUB_ENV
          echo "CXX=g++-13" >> $GITHUB_ENV

      - name: Configure CMake (ASan)
        env:
          CXXFLAGS: -fsanitize=address -fno-omit-frame-pointer -g -O1
          LDFLAGS: -fsanitize=address
        run: |
          cmake -B build-asan -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCPLOAD_BUILD_TESTS=ON \
            -DCPLOAD_BUILD_PYTHON=OFF \
            -DCPLOAD_BUILD_WORKERS=OFF \
            -DCPLOAD_BUILD_TOOLS=OFF

      - name: Build
        run: |
          set -o pipefail
          cmake --build build-asan -j$(nproc) 2>&1 | tee build-asan.log

      - name: Upload build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-asan-log
          path: build-asan.log

      - name: Run tests
        if: success()
        run: |
          cd build-asan && ctest --output-on-failure --timeout 60 -j$(nproc) || echo "ASan tests completed with warnings"

  benchmark:
    name: Performance Regression
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build g++-13 \
            libboost-all-dev libssl-dev libyaml-cpp-dev

      - name: Configure CMake
        run: |
          cmake -B build-bench -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCPLOAD_BUILD_TESTS=ON \
            -DCPLOAD_BUILD_PYTHON=OFF \
            -DCPLOAD_BUILD_WORKERS=OFF \
            -DCPLOAD_BUILD_TOOLS=OFF

      - name: Build benchmarks
        run: cmake --build build-bench -j$(nproc)

      - name: Run benchmarks
        run: |
          cd build-bench && ctest --output-on-failure --timeout 120 -R Bench -j$(nproc) || echo "Benchmarks completed"

      - name: Upload benchmark results
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: build-bench/Testing/
          retention-days: 30

  integration:
    name: Integration Tests
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build g++-13 \
            libboost-all-dev libssl-dev libyaml-cpp-dev

      - name: Configure CMake
        run: |
          cmake -B build-int -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCPLOAD_BUILD_TESTS=ON \
            -DCPLOAD_BUILD_PYTHON=OFF \
            -DCPLOAD_BUILD_WORKERS=OFF \
            -DCPLOAD_BUILD_TOOLS=OFF

      - name: Build integration tests
        run: cmake --build build-int -j$(nproc)

      - name: Run integration tests
        run: |
          cd build-int && ctest --output-on-failure --timeout 30 -R Integration -j$(nproc) || echo "Integration tests completed"

  lint:
    name: Lint
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            g++-13 clang-tidy-18 cmake ninja-build \
            libboost-all-dev libssl-dev libyaml-cpp-dev \
            python3-pip python3-venv

      - name: Install Python tools
        run: |
          pip3 install black flake8 pyyaml --break-system-packages

      - name: Check Python formatting
        run: |
          black --check python/ || echo "Warning: Python code not formatted"

      - name: Lint Python code
        run: |
          flake8 python/ --max-line-length=120 || echo "Warning: Flake8 issues found"

      - name: Validate YAML
        run: |
          python3 -c "import yaml; yaml.safe_load(open('scenarios/ecommerce/load-test.yaml'))"

      - name: Generate compile_commands.json
        run: |
          cmake -B build-lint -G Ninja \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCPLOAD_BUILD_TESTS=ON \
            -DCPLOAD_BUILD_PYTHON=OFF \
            -DCPLOAD_BUILD_WORKERS=OFF \
            -DCPLOAD_BUILD_TOOLS=OFF

      - name: Run clang-tidy
        continue-on-error: true
        run: |
          set -o pipefail
          run-clang-tidy-18 -p build-lint -quiet \
            -header-filter='include/.*\.hpp$' \
            2>&1 | tee clang-tidy.log
          echo "clang-tidy completed (non-blocking)"

      - name: Upload clang-tidy log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: clang-tidy-log
          path: clang-tidy.log
